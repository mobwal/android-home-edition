<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <SwitchPreferenceCompat
        app:key="pin"
        app:summaryOff="@string/pin_settings_summary_off"
        app:summaryOn="@string/pin_settings_summary_on"
        app:title="@string/pin_settings" />

    <EditTextPreference
        app:dependency="pin"
        app:icon="@drawable/ic_baseline_edit_24"
        app:key="pin_code"
        app:title="@string/pin_settings"
        tools:summary="****"
        app:enableCopying="false"
        app:negativeButtonText="@string/undo"
        app:positiveButtonText="@string/yes"/>

</PreferenceScreen>